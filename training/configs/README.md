# Training Configuration Placeholder

Add versioned YAML or JSON templates here only after the continuation-LoRA data contract is fixed. Keep model paths, credentials, and machine-local overrides outside Git.

